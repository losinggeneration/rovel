// Package virtual provides virtualized views for large datasets.
//
// MVP status: unstable pre-v1.
//
// The primary widget is VirtualList, which only paints visible rows and is
// intended to make "render the full history" impossible by default.
package virtual
